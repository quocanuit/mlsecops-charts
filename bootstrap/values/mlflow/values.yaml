service:
  type: ClusterIP
  port: 5000

resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    cpu: 1000m
    memory: 1Gi

extraArgs:
  allowed-hosts: "mlflow.lastdanceuit.io.vn"
  uvicorn-opts: "--log-level=info"